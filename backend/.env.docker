# Flask / Gunicorn
FLASK_ENV=production
BACKEND_HOST=0.0.0.0

# Database
DB_CONNECTION=mysql
DB_HOST=db
DB_PORT=3306
DB_DATABASE=acachat_db
DB_USERNAME=acachat
DB_PASSWORD=supersecret

# JWT / Flask secrets (replace in production)
JWT_SECRET_KEY=change-me-jwt
SECRET_KEY=change-me-secret

# CORS origins (update with your frontend URL)
CORS_ORIGINS=http://localhost:8100

# Gemini
GEMINI_API_KEY=replace-me
GEMINI_MODEL=gemini-2.5-flash

# Auto assign grade defaults
AUTO_ASSIGN_GRADE=true
AUTO_ASSIGN_GRADE_NAME=4to Medio

# Seeder toggle (only true for first run)
RUN_SEED=false
